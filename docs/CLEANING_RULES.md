# Cleaning rules and ETL audit

Generated by `src/08_generate_docs.py` from the `etl_audit` table, so it
cannot drift from what the code actually does. Rules run in order; every
one records rows in, rows out and rows affected. Nothing is dropped without
a row in `dq_quarantine` carrying the original record and a reason.

## Three decisions worth arguing with

**Credit memos are not netted into MRR.** MRR measures the recurring
contract. A one-off, back-dated credit is not a change to that contract.
They live in `fact_credit_memo` and reduce *recognised revenue*, which is
what the cash model consumes. Conflating the two is the most common reason
a SaaS ARR bridge stops tying.

**Missing acquisition channel is never imputed.** Attribution cannot be
reconstructed from revenue. Guessing would feed a fabricated denominator
straight into CAC. Those customers go to an explicit `unattributed` bucket
and are excluded from every channel CAC figure.

**Outliers are flagged, not normalised.** The only outlier treatment applied
anywhere is R05, and only because it has a documented mechanical cause: the
February 2026 billing-engine cut-over double-posted invoices. Every other
extreme movement stays in the history with `outlier_flag = 1`. Removing
unexplained outliers is curve-fitting.

## Rules

| # | Rule | Table | Rows in | Rows out | Affected | Quarantined | Severity |
|---:|---|---|---:|---:|---:|---:|---|
| 1 | **R01** Load raw billing extract | `billing_raw` | 17,310 | 17,310 | 0 | 0 | info |
| 2 | **R02** Quarantine orphan billing rows | `billing_raw` | 17,310 | 17,298 | 12 | 12 | warn |
| 3 | **R03** Separate credit memos from recurring | `billing_raw` | 17,298 | 17,059 | 239 | 0 | info |
| 4 | **R04** Quarantine non-positive recurring lines | `billing_raw` | 17,059 | 17,052 | 7 | 7 | warn |
| 5 | **R05** De-duplicate subscription-month lines | `billing_raw` | 17,052 | 16,895 | 157 | 157 | warn |
| 6 | **R06** Impute missing segment from first-month ARR | `customers` | 826 | 826 | 11 | 0 | info |
| 7 | **R07** Bucket missing channel as 'unattributed' | `customers` | 826 | 826 | 17 | 0 | warn |
| 8 | **R08** Impute missing plan from contracted seat price | `customers` | 826 | 826 | 7 | 0 | info |
| 9 | **R09** Derive dense subscription-month spine and movements | `fact_subscription_month` | 16,895 | 17,181 | 286 | 0 | info |
| 10 | **R10** Flag >5-sigma MRR movements (no removal) | `fact_subscription_month` | 17,181 | 17,181 | 18 | 0 | warn |
| 11 | **R11** Cross-foot marketing spend: GL vs platform export | `fact_marketing_spend` | 216 | 216 | 0 | 0 | info |
| 12 | **R12** Flag pre-window customers as unattributable | `dim_customer` | 826 | 826 | 452 | 0 | warn |
| 13 | **R13** Recognised revenue = MRR less credit memos | `fact_credit_memo` | 239 | 239 | 0 | 0 | info |

## Rule detail

### R01 — Load raw billing extract

Source of record for revenue.

### R02 — Quarantine orphan billing rows

Billing lines whose customer_id has no row in the CRM export. They cannot be attributed to a segment or channel, so they are removed from the facts and held in dq_quarantine.

Quarantined 12 rows. Reason recorded: _customer_id not present in the CRM customer master_

### R03 — Separate credit memos from recurring

Credit memos are one-off adjustments, not changes to the recurring contract. They are excluded from MRR and held in fact_credit_memo, where they reduce recognised revenue only.

### R04 — Quarantine non-positive recurring lines

A recurring subscription line cannot have negative seats. These are data-entry corruption, not contraction — contraction is a *reduction* between two positive months and is derived at R09.

Quarantined 7 rows. Reason recorded: _recurring line with non-positive seats or MRR_

### R05 — De-duplicate subscription-month lines

One row per subscription per month, keeping the earliest created_at. This is the only outlier treatment applied anywhere in the pipeline with a documented mechanical cause: the February 2026 cut-over to the new billing engine double-posted invoices. Concentration by month: {"2026-02": 157}

Quarantined 157 rows. Reason recorded: _duplicate subscription-month line (billing engine migration)_

### R06 — Impute missing segment from first-month ARR

Bands: <$15k SMB, $15k-$60k Mid-Market, >=$60k Enterprise. Flagged in dim_customer.segment_imputed so any analysis can exclude them.

### R07 — Bucket missing channel as 'unattributed'

Attribution is NOT imputed. Guessing a channel from revenue would feed a fabricated denominator straight into CAC. These customers are excluded from every channel CAC and LTV/CAC figure and the exclusion is reported alongside the result.

### R08 — Impute missing plan from contracted seat price

Seat price uniquely identifies the plan in the current rate card.

### R09 — Derive dense subscription-month spine and movements

Each customer gets a contiguous month spine from first to last active month, plus one trailing month so churn is an observable event rather than an absence. movement_type is one of opening_balance / new / expansion / contraction / churn / reactivation / flat and is derived purely from the month-over-month MRR level — never copied from the source system. This is what makes the ARR bridge an actual reconciliation.

### R10 — Flag >5-sigma MRR movements (no removal)

Threshold |delta MRR| > 1,331 (5 x sd of expansion and contraction deltas). These are FLAGGED, not normalised: the only outlier with a documented mechanical cause is the billing migration, already handled at R05. Removing the rest would be curve-fitting, so they stay in the history and carry outlier_flag = 1.

### R11 — Cross-foot marketing spend: GL vs platform export

Marketing-programme lines in opex_ledger must agree with marketing_spend by month x channel. Absolute variance = $0.00. A non-zero variance here means CAC is being built on a different spend number than the P&L.

### R12 — Flag pre-window customers as unattributable

452 customers were acquired before the S&M ledger begins. They carry no acquisition cost and are excluded from every CAC, LTV and payback figure. Including them is the standard way CAC gets flattered — the denominator grows, the numerator does not.

### R13 — Recognised revenue = MRR less credit memos

Total credit memos across the window: $-92,785.55 over 239 lines. Recognised revenue feeds the cash model; MRR feeds the ARR bridge and retention metrics. They are different numbers on purpose.

