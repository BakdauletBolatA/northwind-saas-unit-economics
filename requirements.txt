# Northwind Cloud unit-economics pipeline
pandas>=2.0
numpy>=1.24
openpyxl>=3.1
PyYAML>=6.0
# SQLite ships with the standard library.
# LibreOffice (soffice) is required by src/06_validate.py to recalculate the
# workbook headlessly. On Debian/Ubuntu: apt-get install -y libreoffice-calc
